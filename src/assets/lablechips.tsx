import React from 'react';

const LabelChips = ({ children }) => (
  <div className="inline-block rounded-full bg-[#122947] px-2 py-[0.3125rem] text-[11px] font-medium leading-[16px] text-white">
    {children}
  </div>
);

export default LabelChips;